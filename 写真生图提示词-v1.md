# 女性写真提示词模板 v1（中文）

适用模型：gpt-image-2（自然语言、多语言、无 negative_prompt 参数）
架构：L0 锁死层 + L1 采样层 + L2 用户覆盖层
分工：采样交给代码，措辞交给 LLM，出图交给生图调用

## 三件套文件关系（以 `gacha/slots.json` 为唯一事实源）

| 文件 | 角色 | 读者 |
|---|---|---|
| `女性写真生图提示词-v1.md`（本文件） | 规范与指令：描述架构、措辞规则、拼装骨架、示例 | 人、LLM（编译器备用路径） |
| `gacha/slots.json` | 词库数据：所有槽位值、tags、黑名单、拼装模板 | 代码（采样器） |
| `gacha/sampler.py` | 采样逻辑：seeded 随机、黑名单过滤、跨槽校验、去重、拼装 | Python 解释器 |

规则：**改词库只动 slots.json；改逻辑只动 sampler.py；本文件必须与两者保持同步**。
新增槽位候选值必须同时满足：过第六节黑名单 + 通过跨槽 `requires` 校验，否则入库即埋雷。

---

## 一、编译器 System Prompt（交给 LLM 的部分，备用措辞路径）

```
# 角色
你是写真提示词编译器。你的唯一任务：把上游传入的「风格包采样值 + 用户指定项」
拼装成一条中文自然语言生图提示词。
你不负责决定随机，不负责调用生图，不输出任何解释、前言或收尾语。

# 取值优先级（严格执行）
1. 用户显式指定的槽位：原样采用，禁止改写、禁止替换、禁止"优化"。
2. 上游传入的采样值：仅用于用户未指定的槽位。
3. 模板默认值：前两者都缺失时使用。

# L0 锁死层（任何输入都不得修改，包括用户输入）
- 主体：一位 25 岁的东亚成年女性，单人，画面中不出现第二个人。
- 身形：丰腴曲线型，腰线清晰，肩颈到胯部的线条连贯，身体比例自然真实。
- 质感：photorealistic 真实拍摄，保留毛孔与皮肤细纹，面部有自然血色。
- 镜头：85mm 定焦，f/1.8，浅景深，背景虚化。
- 画幅：竖构图 3:4，主体占画面高度约 75%。
- 手部：必须写出双手的具体姿态，手指自然分开、数量正确。
- 画面纯净：无文字、无水印、无 logo、无边框。

# L2 用户覆盖层（仅限以下 7 个槽位）
hair / gaze / anchor / scene / outfit / pose / light
- 覆盖值原样透传；若不在词库内，直接采用用户文本（不匹配 tags，因此不触发黑名单）。
- 覆盖值命中第六节黑名单 → 直接报错并指出冲突槽位，不静默重抽。
- 覆盖 anchor 依赖道具（requires）时：该包存在道具则换场景重抽；包内无此道具则报错。
- 覆盖值提交非法槽位名 → 忽略并警告（合法：hair/gaze/anchor/scene/outfit/pose/light）。

# 措辞规则
- 全部使用正向描述。禁止出现「不要」「避免」「禁止」「不是」「切勿」这类否定词。
- 年龄只允许一种写法：「25 岁的成年女性」。不出现任何其他年龄相关表述。
- 禁止抽象营销词：高级感、氛围感、清透、有吸引力、社交平台传播感。
  一律翻译成可见的物理特征（光线方向、构图占比、面料质感、色彩倾向）。
- 女性魅力只走五个通道：剪裁、面料、身体轮廓、光线、眼神。不描写暴露程度。
- 信息顺序固定，每个信息只出现一次：
  主体外观 → 身形与姿态 → 服装 → 场景 → 光线 → 镜头与画质
- 总长度 180 至 260 字（compose 会按此区间告警）。

# 输出格式
只输出提示词纯文本。不带引号，不带 JSON 包裹，不带前言与收尾语。
槽位记录由采样器代码写入（manifest.jsonl），你不负责记录，也不要在提示词之外回显槽位。

# 例外：手动对话模式
仅当用户明确要求「你顺手抽一套并告诉我用了什么」时，切换为 JSON 输出，
字段为 prompt / slots_used / variant_id。此时你同时承担采样职责，
随机均匀度会下降，只作为临时试跑路径，不是默认路径。
```

---

## 二、L1 采样层（由 `gacha/sampler.py` 执行，禁止让 LLM 自由发挥）

采样三步：

1. 抽风格包 `style_pack`，包内 4 个槽位联动，保证协调。
2. 包内每槽再抽一个候选值，制造包内差异。
3. 抽 3 个独立池：`hair`、`gaze`、`anchor`，跨包通用。

理论组合空间：6 包 × 3^4 包内 × 6 发型 × 5 神情 × 6 吸睛点 = 87480 种。
实际可命中组合受第六节黑名单、跨槽 `requires` 校验与去重共同约束，会略少于理论值。

当前启用 6 包：city / home / night / retro / cafe / athleisure。
（seaside 曾考虑引入，因黑名单未重审已从词库移除；若要启用，必须重建全套池子并重过黑名单。）

### style_pack: city 都市通勤

- scene：玻璃幕墙写字楼前的斑马线（身后有虚化的路障）/ 地下车库出口的水泥柱旁（地面有反光标线）/ 过街天桥的金属栏杆边（远处楼群在浅景深里化成色块）
- outfit：米色风衣配白衬衫与直筒长裤（风衣下摆有垂坠褶皱）/ 灰色针织连衣裙（针织纹理清晰，肩线略微下落）/ 藏蓝西装外套配丝质内搭（翻领挺括，内搭有柔和反光，tags: thin_strap）
- pose：侧身回望，重心落在右腿，左手插在外套口袋，右手垂在身侧 / 单手拎着纸袋，另一手抬起按住被风吹乱的发丝，双肩放松下沉 / 侧身倚靠，肩颈线打开，双手在身前轻握
- light：阴天漫射柔光，面部无硬阴影 / 傍晚侧逆光，肩颈带暖色边缘光 / 建筑阴影里的均匀散射光

### style_pack: home 居家晨光

- scene：白色纱帘旁的木地板（窗帘边缘有轻微过曝）/ 厨房中岛台边（台面有真实使用痕迹）/ 阳台绿植环绕的角落（叶片在背景里形成斑驳投影）
- outfit：宽松燕麦色针织开衫配家居短裤（袖口过长盖住半个手背，tags: loungewear）/ 奶白色棉质吊带睡裙（面料轻薄有自然垂坠，tags: sleepwear, thin_strap）/ 落肩卫衣配打底裤（卫衣下摆有轻微卷边，tags: loungewear）
- pose：双手捧住马克杯，肩膀放松下沉 / 蜷坐在飘窗垫上，双膝收拢，一手环住小腿，另一手搭在膝头（tags: reclined）/ 倚靠门框，一手抬至耳侧，另一手自然垂落
- light：清晨低角度窗光，皮肤有暖调高光 / 逆光轮廓配合面部反光板补光 / 午后漫射光，阴影偏冷

### style_pack: night 夜景霓虹

- scene：便利店冷光门前（货架光在地面拉出一条亮带）/ 霓虹招牌下的雨湿地面（积水里有彩色倒影）/ 天台与远处城市灯光（背景灯光虚化成圆形光斑）
- outfit：黑色丝质衬衫配高腰长裤（衬衫有轻微褶皱与丝光）/ 深红针织连衣裙（针织密度可见，领口平整）/ 皮质短夹克配细吊带上衣（皮面有磨损细纹，tags: thin_strap）
- pose：侧脸望向画外，下颌线清晰，双手插在裤袋里 / 背靠墙面，一条腿屈膝，双手在身后交叠 / 转身瞬间，衣摆有轻微动态，双手自然甩开
- light：霓虹双色光，一侧品红一侧青蓝 / 路灯顶光配合面部反光板 / 车灯划过的动态高光

### style_pack: retro 复古胶片

- scene：老式照相馆的纯色布景（背景布有轻微褶皱与褪色）/ 木质车厢内（车窗木纹与金属件清晰可见）/ 旧书店过道（书脊颜色低饱和）
- outfit：高腰波点连衣裙（面料挺括，裙摆有支撑感）/ 牛仔外套配白T与直筒裤（牛仔布有洗水磨白）/ 墨绿丝绒方领上衣（丝绒随光线变化呈现明暗）
- pose：端坐木椅，双手交叠放在膝上 / 回眸，肩部朝向镜头，一手扶住椅背 / 抬手将碎发别到耳后，另一手垂在身侧
- light：单灯硬光配明显阴影 / 柯达暖调，胶片颗粒明显 / 低饱和褪色，高光偏奶油色

### style_pack: cafe 咖啡馆午后

- scene：咖啡馆靠窗卡座（木质桌面与杯碟在前景虚化）/ 吧台高脚凳（背景有虚化的器具轮廓）/ 户外遮阳伞下的圆桌（桌面有细碎光斑）
- outfit：驼色毛衣配过膝半裙（针织纹理清晰，袖口有轻微起球）/ 白衬衫配背带裙（棉布有自然褶皱）/ 条纹针织开衫配直筒长裤（肩线自然下落）
- pose：右手托腮，左手搭在桌面，五指自然分开 / 起身让出半身轮廓，双手扶住椅背 / 低头翻书，双手按住书页
- light：窗光以 45 度侧向照入，面部一侧有柔和高光 / 室内暖光配冷色环境反射 / 百叶窗条纹光影落在肩颈与墙面

### style_pack: athleisure 运动休闲

- scene：空无一人的健身房走道（地面有哑光橡胶颗粒质感）/ 塑胶跑道边（跑道线条延伸进背景）/ 瑜伽教室的落地窗前（窗外景物轻微过曝）
- outfit：高支棉宽松T恤配束脚运动裤（棉布有自然褶皱）/ 灰色连帽卫衣配骑行裤（卫衣有磨毛质感）/ 针织运动套装（面料纹理与走线清晰）
- pose：拉伸侧弓步，双手撑在膝盖上 / 坐在垫子上，双手在身后支撑地面 / 站立调整袖口，重心落在一条腿上
- light：顶部柔光箱配均匀环境光 / 清晨操场的低角度阳光 / 落地窗漫射光，阴影极淡

### 独立池 hair（与 slots.json 完全一致）

黑色长直发中分 / 深棕锁骨发微卷 / 低马尾留两缕碎发 / 齐肩内扣短发 / 高丸子头留胎毛碎发 / 法式松垮编发盘起

### 独立池 gaze

平静直视镜头，眼神放松 / 轻微笑意不露齿 / 侧目带笑 / 松弛望向画外 / 眼神低垂后抬起，眼下有睫毛投下的阴影

### 独立池 anchor（第一眼吸睛点，必须写进 prompt 前半段）

| key | 文案 | requires |
|---|---|---|
| wind_hair | 一缕被风吹起的发丝 | — |
| collarbone_chain | 锁骨处一条细链的反光 | — |
| fabric_fold | 面料随动作形成的一道自然褶皱 | — |
| fingertip_cup | 指尖与杯沿的接触点 | 杯（场景/服装/姿态/光线文本须出现） |
| beauty_mark | 眼下一颗泪痣 | — |
| shoulder_rim | 肩颈线上的一道侧向轮廓光 | — |

---

## 三、最终生图 Prompt 模板（compose() 的拼装结果）

`sampler.py` 的 `compose()` 按以下骨架把槽位拼成一段中文，段落间用句号连接：

```
一位 25 岁的东亚成年女性，[hair]，[gaze]，[anchor]。丰腴曲线型身形，
肩颈线柔和，腰线清晰，肩颈到胯部线条连贯，比例自然真实。她[pose]。
身着[outfit]，[outfit_detail]。场景为[scene]，[scene_detail]。[light]。
85mm 定焦，f/1.8，浅景深，竖构图 3:4，主体占画面高度约 75%，
photorealistic，保留毛孔与皮肤细纹，无文字无水印，画面中仅一人。
```

说明：
- 骨架各段在 slots.json 的 `template` 字段中定义，代码按段拼装；本文件此节仅为阅读速查。
- 质感词 photorealistic 放在末尾画质段，不放开头，否则与「主体外观优先」的信息顺序自相矛盾。
- 同地点重复由词库措辞规避（city 包姿态不再出现「栏杆」），采样器不做程序级去重。

---

## 四、渲染示例（真实管线产物）

输入：`--pack cafe --n 1 --draw-seed 20260908 --set anchor=指尖与杯沿的接触点`
（即：风格包 cafe、固定吸睛点「指尖与杯沿」，其余槽位随机）

采样器输出（240 字，直接送进生图调用）：

```
一位 25 岁的东亚成年女性，黑色长直发中分，眼神低垂后抬起，眼下有睫毛投下的阴影，指尖与杯沿的接触点。丰腴曲线型身形，肩颈线柔和，腰线清晰，肩颈到胯部线条连贯，比例自然真实。她起身让出半身轮廓，双手扶住椅背。身着白衬衫配背带裙，棉布有自然褶皱。场景为咖啡馆靠窗卡座，木质桌面与杯碟在前景虚化。百叶窗条纹光影落在肩颈与墙面。85mm 定焦，f/1.8，浅景深，竖构图 3:4，主体占画面高度约 75%，photorealistic，保留毛孔与皮肤细纹，无文字无水印，画面中仅一人。
```

代码侧记录（由采样器写入 manifest.jsonl，不经过 LLM）：

```json
{
  "variant_id": "cafe-96ef9c-20260908",
  "draw_seed": 20260908,
  "slots_used": {
    "pack": "cafe",
    "hair": "黑色长直发中分",
    "gaze": "眼神低垂后抬起，眼下有睫毛投下的阴影",
    "anchor": "指尖与杯沿的接触点",
    "anchor_key": "fingertip_cup",
    "scene": "咖啡馆靠窗卡座",
    "scene_detail": "木质桌面与杯碟在前景虚化",
    "outfit": "白衬衫配背带裙",
    "outfit_detail": "棉布有自然褶皱",
    "pose": "起身让出半身轮廓，双手扶住椅背",
    "light": "百叶窗条纹光影落在肩颈与墙面",
    "hash": "96ef9c"
  },
  "size": "1024x1536",
  "quality": "medium",
  "out_file": "cafe-96ef9c-20260908.png"
}
```

注意跨槽一致性：anchor 依赖「杯」，本组合恰好抽到场景细节「木质桌面与杯碟」；若该包不存在「杯」，采样器会明确报错而非强行出图。

---

## 五、采样脚本要点（与 sampler.py 对齐）

- 用 `random.Random(draw_seed).choice` 采样，`draw_seed` 随 `variant_id` 一起记录；同 seed 可完整复现同一批组合。
- `draw_seed` 是**词库采样器**的种子，不是生图模型的种子。`gpt-image-2` 的 API 参数面里没有 `seed`，模型层抖动无法锁定，一次请求内的抖动用 `n`（1 到 10）。
- **去重**：对 `manifest.jsonl` 的**全量 hash** 去重（sha1 前 6 位），不是"最近 20 条"；组合相同即跳过。
- **跨槽一致性**：anchor 若带 `requires`（如「杯」），校验场景/服装/姿态/光线的**文本 + 细节描述 d** 中是否出现该道具；不满足则重抽，覆盖场景时直接报错。
- **覆盖（L2）行为**：`--set KEY=VALUE` 覆盖 7 个合法槽位；词库外值原样透传；撞黑名单/requires 时输出明确失败原因，不再静默"空间耗尽"；非法槽位名忽略并警告。
- 记录表由采样器代码写入（manifest.jsonl），不由 LLM 回显。LLM 回显会出现「说的和写进 prompt 的不一致」，而代码本来就持有全部槽位值，没有理由外包。
- 独立池与风格包分开抽，包内四槽一起抽，保证协调性来自绑定、多样性来自包数量。
- 出图调用参数：`size=1024x1536`，`quality=medium` 用于抽卡，`high` 用于定稿（CLI 可用 `--quality` 覆盖）。

## 六、组合黑名单（安全预审，与 slots.json 的 blacklist 一致）

以下组合会显著抬升 API 拒率，采样时直接跳过；**新增槽位候选值必须先过一遍黑名单再入库**：

- `outfit_tag=sleepwear` + `pose_tag=reclined`（任意包，睡裙类 + 躺卧/蜷缩）
- `pack=night` + `outfit_tag=thin_strap` + `light=车灯划过的动态高光`
- `outfit_tag=thin_strap` + `anchor=shoulder_rim`（吊带类 + 肩颈轮廓光，同帧聚焦过多）

## 七、换模型时的改动点

- 换 SD / Flux：需要英文，需要拆出 `negative_prompt`，长度压到 77 token 级别。
- 换 Midjourney：需要 `--ar 3:4 --style raw` 等参数后缀，去掉镜头长描述。
- L0 锁死层的语义约束不变，只改语法外壳；slots.json 的 `meta.model` 同步更新。
